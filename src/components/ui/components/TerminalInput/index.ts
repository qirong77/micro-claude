/**
 * TerminalInput UI 组件
 *
 * 导出: { renderFn, emitter, atomText }
 * - renderFn: React 渲染函数
 * - emitter: mitt 事件发射器 (submit 事件)
 * - atomText: nanostores atom (响应式输入文本)
 */
export { TerminalInputUI } from './TerminalInput.js';
